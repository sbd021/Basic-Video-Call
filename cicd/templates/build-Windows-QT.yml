# .NET Desktop
# Build and run tests for .NET Desktop or Windows classic desktop solutions.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/windows/dot-net

parameters:
  displayName: ''
  workingDirectory: ''
  scheme: ''
  sdkurl: ''
  solutionName: ''
jobs:
  - job: ${{ parameters.displayName }}Build
    displayName: ${{ parameters.displayName }}

    pool: 'Default'

    variables:
      solution: '${{ parameters.workingDirectory }}/${{ parameters.solutionName }}'
      buildPlatform: 'win32'
      buildConfiguration: 'Release'
      
    steps:
      - script: echo "${{ parameters.sdkurl }}"
      - powershell: cicd/scripts/downloadsdk.ps1 "${{ parameters.sdkurl }}"
   
      - task: ExtractFiles@1
        inputs:
          archiveFilePatterns: 'AgoraSDK.zip' 
          destinationFolder: 'AgoraSDK'
          
      - task: CopyFiles@2
        inputs:
          sourceFolder: 'AgoraSDK/Agora_Native_SDK_for_Windows_v2.9.3_FULL/sdk'
          targetFolder: ${{ parameters.workingDirectory }}/sdk

      - script: cd ${{ parameters.workingDirectory }} 
						- script: ${{ parameters.workingDirectory }}/build_azure_pipeline.bat
 # script: call ${{ parameters.workingDirectory }}/build_release.bat "C:\\Program Files (x86)\\Microsoft Visual Studio\\2017\\Community\\VC\Auxiliary\\Build" "C:\\Qt\\Qt5.12.7\\5.12.7\\msvc2017" x86 "${ parameters.workingDirectory }"